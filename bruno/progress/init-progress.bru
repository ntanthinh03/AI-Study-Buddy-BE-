meta {
  name: Init Progress
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/progress/init
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "documentId": "{{documentId}}"
  }
}
